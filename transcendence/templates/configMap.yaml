apiVersion: v1
kind: ConfigMap
metadata:
  name: transcendence-config
  namespace: {{ .Values.global.namespace }}
data:
{{- range $key, $val := .Values.configMap }}
  {{ $key }}: {{ $val | quote }}
{{- end }}