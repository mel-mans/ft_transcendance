apiVersion: v1
kind: Service
metadata:
  name: user
  labels:
    app: user-service
spec:
  type: ClusterIP
  selector:
    app: user
  ports:
    - protocol: TCP
      port: {{ .Values.backend.user.service.port }}
      targetPort: {{ .Values.backend.user.service.port }}
